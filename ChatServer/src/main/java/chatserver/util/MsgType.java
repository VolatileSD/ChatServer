package chatserver.util;

public enum MsgType {

   DATA, COMMAND, EOF, IOE, ENTER, LEAVE, LINE,
   ROOM_INFO, CREATE_ROOM, DELETE_ROOM, // REST
   OK, INVALID, UNAUTHORIZED, AUTH,
   CREATE, CREATE_ADMIN, REMOVE, LOGIN, LOGIN_OK, LOGOUT,
   PRIVATE, INBOX, ALL_USERS, INBOX_USERS, TALK,
   CHANGE_ROOM,
   RESTORE
}
